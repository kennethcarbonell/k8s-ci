This file includes the initial deployment of the app for Kubernetes.

Note that you must change the image name to match the user. There is also a field "imagePullPolicy" which always pulls the latest image.